1. En una lista, escriba sus 3 amigos preferidos, usando el ciclo for muestrelos por pantalla.

CICLO FOR RANGE
for i in range(5):
    print(i)

for = ciclo

i = variable para iterar

range(5) = elemento iterable
